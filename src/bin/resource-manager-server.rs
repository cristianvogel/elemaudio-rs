fn main() {
    println!("resource-manager-server lives in the public elemaudio-resources repo.");
    println!(
        "Run it with: `cargo run --manifest-path ../elemaudio-resources/Cargo.toml --bin resource-manager-server`"
    );
    println!("Or use: `./scripts/dev-all.sh` from the elemaudio-rs repo root.");
}
